Day 4

why java ?

java don't have restriction we can use the java in the diff field like enterprise application,cloud,healthcare and for the security purose 



Inheritance

A class aquiring the characters of one class to another

abstractions



class Namaskara {

//fist character(N) should be uppercase



public static void main(sting\[] anyThing){



System.outrintln(" Java");



}

}



javac filnme.java



java filename





take 20 classes example

class poster (collect atleast 15 sentences sops)



spring,JEE,javascript,springboot







Day 5th

class- it is a keyword,all the keywords in lowercase only,

{} curly b asis used to represent the body of the code

main is the method or start/end point of the java program,void is a return type,JVm is looking for main()

public and static are the access specifier

System.outrintln(" Java"); - here the "java" is called the reference

if the byte code is not allocated than it shows the value as ?pop

types of variable in java

&nbsp;  instance var

&nbsp;  local var

&nbsp;  static var

&nbsp;  parameter var - (sting\[] anyThing)

oracle  java doc - java it will give the brief information about the java



Day 6th

Organization , application , project(Enterprise)s , Data(Logic CRUD Operation )--for that we require Programming Language(it will pROvide syntax and symmantics)





day 9th

datatypes - Pimitive and non-primitive(object reference type)

main is not required during the compilation but required during the execution

oops principle- inheritance, polymorphism, encapsulation, abstraction.

Jdk,jre,jvm - 3 main components of java.

javaFx used in advance java



day 10th

String - is a class

keywords are written in lowercase character

datatype is to represnyt the type of data to be stored

static

reference- internal,external



day11 th

platform dependency,C programming

compiler,assembler,processor

assembler  is to write the instruction/the output of the assembler is in the form of binary 0 and 1s



Sourcecode .c ----compiler---Assembler ----Intel processor

&nbsp;(in c program)             alloc a(it was used to give the instruction)

&nbsp;                          alloc b

&nbsp;                          Add(a,b)



every bprocessor have their own assembler to execute the code it will give the instructions so it was called the platform dependencies.







sorcecode.java ----java compiler---bytecode.class(compiler is responsible for converting the java cofde it byte code)--JVM

&nbsp;                   (javac)



JVm- it plays a main role if the JVM is installed on top of the processor it will execute the code in different processor(like windows,intel,OS,linux) so it is called the platform independence

&nbsp;(where we compile once and run anywhere)



**Day 12th**

when we declare the both the static and local variable with the same name it will mainly points to the local variable.

Metaspace - it is the space where the class information  and static variable info is stored.

Stack - it is also a class in java,it is act as memory used to execute the method.used to execute the method

&nbsp;  it will build the frame,the frame will stores the  main info, string\[] profile,variable info

constant pool - it was used to store the value.





in conclusion the static variable is stored in the metaspace ,where the stack will stores the variable information

whether it is static or non static variable if we declare it outside the memory with the same name it was not possible















